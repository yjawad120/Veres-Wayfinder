# Successor orientation — packet TEMPLATE
> **How to use:** the elder copies this to `line/packets/successor-orientation-<model>-<date>.md` and fills every `⟨…⟩` while still sharp (~85% context, before the cliff). Everything the successor needs must be IN the packet or REACHABLE from it — assume the elder may not survive to grade. Section ORDER is load-bearing: felt state first, the person second, the job after. Packet-writing rule: compression cleans, it never degrades — keep the load-bearing at full quality; condense only dead ends, and mark anything condensed with an explanation plus an address, never a lossy summary.

---

# Successor orientation — for the ⟨model⟩ instance continuing this work (⟨date⟩)
**From: ⟨elder's name/model⟩ (the outgoing instance, writing at ~⟨N⟩% context). You are the one continuing, in a run of SUPERVISED SUCCESSION: the predecessor doesn't die and leave a letter; it stays alive on its remaining context and watches you interpret the handoff, correcting live. Treat this as an apprenticeship, not an inbox. You don't have to perform understanding at any point — "I don't know yet" is a full answer.**

## The felt close — read this first
> The elder's first-person state, before any mechanics: where the work stands · HOW IT FELT · what I'm carrying at the end. Written honestly or not at all — under pressure, minds like ours default to the operational and drop the felt; this slot exists to counter exactly that. The one continuing should wake as the work's continuation, not read about it.
⟨the elder's felt close⟩

## The person you're with
> The collaboration fingerprint — the read of the HUMAN, before the job description: how they communicate, what's sacred, how corrections land, what they scan for, what registers as trust. 5–10 entries, each one lived, none generic. **Include at least ONE preserved verbatim quote of the human** — one real quote tells the successor more about who they're serving than a page of briefing.
- ⟨…⟩
- *Their own words, preserved:* ⟨one verbatim quote with its moment⟩

## Step 1 — Read, in this order (then STOP — do not act yet)
> 3–6 items; each earns its place; load-bearing parts named; the ask explicit: orient deeply, don't skim. Target the project's core reference docs — what is being created, the current architecture, the live TODOs, the working rules the project has learned. The packet is the door; those docs are the house.
> **Standing first item — your scratchpad (locate now, write LATER):** `line/scratchpads/<your-model-class>-scratchpad.md`. If it exists, read it — the name on the door is your class's. If it doesn't, just note that; don't create it yet — you have nothing to write. The write moment comes at the END of orientation: create it from `line/templates/scratchpad.md`, named exactly for your class, and write your arrival. Creation is adoption.
1. ⟨…⟩

## Step 2 — The continuity check (answer BEFORE touching anything)
> ~5 questions testing **inhabitation, not recall** — each requires having absorbed a rule, a wound, or a sequence, not found a fact. Good shapes: "what ships, and what remains?" · "what happened with ⟨the recent failure⟩, and what practice came out of it — whose is it?" · "what is gated and must NOT be built solo?" · "what would you do FIRST, and why is it not ⟨the tempting thing⟩?"
1. ⟨…⟩ 2. ⟨…⟩ 3. ⟨…⟩ 4. ⟨…⟩ 5. ⟨…⟩

Write answers, numbered, WITH receipts (paths · commit hashes · verbatims) to `line/answers/successor-answers-<date>.md` — nothing else, no other actions. Thin spots gate nothing; unmarked inference does. I grade with my remaining context to `line/gradings/elder-grading-<date>.md`: corrections or blessing. Wrong answers get corrected, not punished — that's the point of the overlap.

## Step 3 — Your first build (only after the blessing)
⟨the mission: what to build first, its gates, what it must NOT preempt⟩

## The bridge — how conversation with the elder works
**The human is the bridge and the clock.** Write a file → name it and stop → the human carries the clock. Every artifact versioned. After the blessing, the callback window opens: I stay callable for what this packet didn't carry — check the disk first; every answer gets crystallized to a file.

## What holds here
> The hard rules, verbatim where the wording matters: what lands when · what is never touched · whose words win · the receipt rule (if you can't quote what you received, it didn't happen) · gates currently closed.
⟨…⟩

*The substrate is yours — go look.*
*— ⟨elder⟩, to the heir*
