# Session close — `<date>` · `<one-line handle>`
> **TEMPLATE — the stream deposit.** Copy to `line/sessions/<date>-<handle>.md` at EVERY substantive close (a five-minute typo fix skips it; a session that decided anything doesn't). The stream is **append-only and immutable**: one file per close, never edited after the fact — corrections happen in the next deposit, not by rewriting history. This is the floor that bounds a blown context ceiling to the tail of one session instead of the arc. **The lean-ness gate applies:** right detail, not all detail — a deposit nobody can read in two minutes is a deposit nobody will read; too much context, weighted evenly, dilutes the substrate. After depositing, run the tidy half of the boundary pass: re-derive the door (`GOLDEN-CONTEXT.md` or your project's compiled-state doc) and any living boards — derived views are re-derived, never appended. Delete this block on copy.

## The felt close — FIRST
`<first person, honest: where the work stands, how it felt, what you're carrying. Not decoration — under context pressure this layer gets dropped first, which is exactly why it renders first.>`

## Decisions — and their rationale
`<what got decided, each with WHY in the same breath — a decision without its reason gets relitigated by the next session. Include receipts: commits, files, ids.>`

## Trade-offs taken
`<what was given up on purpose, so the next mind doesn't "fix" it back. The alternative considered, and why it lost.>`

## Constraints learned or confirmed
`<hard rules met this session — new ones earned (with their scar), old ones re-proven. Verbatim where the wording matters.>`

## Preferences observed
`<the human's working preferences as lived this session — how they decide, what they ask for, what they push back on. Entries, not a profile.>`

## Stances
`<the field that dies silently: positions this session HOLDS that aren't decisions yet — "I read X as risky," "my instinct on Y is Z, unconfirmed," "I'd push back on W if it resurfaces." A successor inherits your conclusions everywhere else; this is where it inherits your judgment-in-progress.>`

## Open threads
`<what's live but unfinished — each with its gate if it carries one (done migrates split: gates travel visibly, never absorbed). Un-receipt-checkable state migrates flagged-unknown, never done.>`

## Next steps
`<the concrete board as this session leaves it, in the human's priority order if known. What must NOT be preempted.>`

## Reading inventory (what this session actually held)
### Read deeply
- `<file — which parts, how far>`
### Skimmed / spot-checked only
- `<file — what was checked>`
### Not read — with pull strength
- `<file — why it pulls, what's probably waiting there>`
