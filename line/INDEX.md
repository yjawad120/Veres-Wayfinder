# The run index — one line per succession
> Append at every close: `run id · date · elder → successor · outcome (blessed / degraded-mode / aborted at stage N)`. An archive without an index is dead information.

| run | date | elder → successor | outcome |
|---|---|---|---|
| _(your first run goes here)_ | | | |
